{% extends 'base.html.twig' %}
{% block title %}Contactez nous{% endblock %}
{% block body %}
    <div class="contactForm">
    <h1>Prenez contact avec Mina Louve Doulà</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
    <button class="sendBtn">{{ button_label|default('Valider') }}</button>
    {{ form_end(form) }}
</div>
{% endblock %}